
# 8장: HTML DOM과 Document



## 1. HTML DOM (Document Object Model)

**DOM (Document Object Model)**은 웹 페이지의 HTML 태그들을 자바스크립트가 제어할 수 있도록 객체로 만든 모델입니다.

* **목적**: 자바스크립트를 이용해 HTML 태그의 콘텐츠나 스타일을 동적으로 제어하기 위함입니다.
* **DOM 트리**: HTML 태그들의 포함 관계(부모-자식)를 나무(트리) 구조로 표현한 것입니다.
* **DOM 객체**: DOM 트리를 구성하는 각 노드(node)로, HTML 태그 하나당 하나의 DOM 객체가 생성됩니다.

## 2. DOM 객체의 구성 요소

HTML 태그는 5가지 요소를 가지며, DOM 객체는 이를 반영하여 5가지 구성 요소를 가집니다.

1.  **프로퍼티 (Property)**: HTML 태그의 속성(attribute)을 반영합니다. (예: `id`, `tagName`, `innerHTML`)
2.  **메소드 (Method)**: DOM 객체를 제어하는 함수입니다. (예: `setAttribute()`)
3.  **컬렉션 (Collection)**: `children`과 같이 자식 DOM 객체들의 리스트를 가지는 배열과 유사한 정보입니다.
4.  **이벤트 리스너 (Event Listener)**: `onclick` 등 HTML 태그에 작성된 이벤트를 반영합니다.
5.  **CSS3 스타일**: `style` 프로퍼티를 통해 태그의 CSS 스타일을 접근하고 변경할 수 있습니다.

## 3. DOM 객체 다루기

### 🎨 DOM 객체 찾기

자바스크립트가 DOM 객체를 제어하려면 먼저 원하는 객체를 찾아야 합니다.

* `document.getElementById("id")`: **id 속성**으로 DOM 객체 하나를 찾습니다. (가장 많이 사용)
* `document.getElementsByTagName("tag")`: **태그 이름**으로 모든 DOM 객체를 찾아 컬렉션(HTMLCollection)으로 반환합니다.
* `document.getElementsByClassName("class")`: **class 속성**으로 모든 DOM 객체를 찾아 컬렉션으로 반환합니다.

### 🎨 콘텐츠 및 스타일 변경

* **콘텐츠 변경 (`innerHTML`)**: `innerHTML` 프로퍼티는 태그의 시작과 종료 태그 사이의 HTML 콘텐츠를 나타냅니다. 이 값을 변경하면 화면의 내용이 바뀝니다.
    ```javascript
    let p = document.getElementById("firstP");
    p.innerHTML = "새로운 <b>내용</b>입니다."; //
    ```
* **스타일 변경 (`style`)**: `style` 프로퍼티를 통해 CSS 스타일을 동적으로 변경할 수 있습니다.
    * CSS 속성명이 `background-color`처럼 하이픈(-)을 포함하면, `backgroundColor`와 같이 카멜 케이스(camelCase)로 변경해야 합니다.
    ```javascript
    let span = document.getElementById("mySpan");
    span.style.color = "green";
    span.style.fontSize = "30px";
    ```
* **`this` 키워드**: 이벤트 리스너에서 `this`는 이벤트가 발생한 **객체 자신**을 가리킵니다.
    ```html
    <div onclick="this.style.backgroundColor='orange'">
      여기를 클릭하세요
    </div>
    ```

## 4. `document` 객체

`document` 객체는 HTML 문서 전체를 대표하는 객체로, DOM 트리의 최상위 루트입니다.

### 📄 `document.write()`

`document.write()`는 HTML 페이지가 로드되는 **중에** `document` 객체에 HTML 콘텐츠를 추가합니다.

> **⚠️ 주의:** 페이지 로딩이 **완료된 후** (예: 버튼 클릭 시) `document.write()`를 호출하면, 기존 문서의 모든 내용이 지워지고 새로운 내용으로 덮어 써집니다.

### 📄 동적 DOM 생성 및 삭제

자바스크립트로 HTML 태그(DOM 객체)를 새로 만들거나 삭제할 수 있습니다.

1.  **생성**: `document.createElement("태그이름")`
    * `let newDIV = document.createElement("div");`
2.  **속성/내용 설정**:
    * `newDIV.innerHTML = "새로 생성된 DIV";`
    * `newDIV.style.backgroundColor = "yellow";`
3.  **트리에 삽입**: `부모객체.appendChild(자식객체)`
    * `document.body.appendChild(newDIV);` (body 태그의 마지막 자식으로 추가)
4.  **삭제**: `부모객체.removeChild(자식객체)`
    * `let parent = myDiv.parentElement;`
    * `parent.removeChild(myDiv);`
  














# 9장: 이벤트 기초 및 활용


## 1. 이벤트란?

* [cite_start]**이벤트 (Event)**: 마우스 클릭, 키보드 입력, 페이지 로딩 완료 등 사용자의 행위나 브라우저의 상태 변화를 알리는 **통지(notification)**입니다. [cite: 899]
* [cite_start]**이벤트 리스너 (Event Listener)**: 이벤트가 발생했을 때 실행되도록 작성된 자바스크립트 코드(함수)입니다. [cite: 901]
* [cite_start]**이벤트 리스너 이름**: 이벤트 이름 앞에 `on`을 붙입니다 (예: `click` 이벤트 → `onclick` 리스너). [cite: 903, 905]

## [cite_start]2. 이벤트 리스너 등록 방법 3가지 [cite: 947]

1.  **HTML 태그의 속성으로 작성**:
    ```html
    <p onmouseover="this.style.backgroundColor='orchid'">
      마우스 올리기
    [cite_start]</p> [cite: 954, 955]
    ```
2.  **DOM 객체의 프로퍼티로 작성**:
    * 자바스크립트에서 DOM 객체의 이벤트 리스너 프로퍼티에 함수를 할당합니다.
    ```javascript
    let p = document.getElementById("p");
    p.onmouseover = over; [cite_start]// over라는 함수를 리스너로 등록 [cite: 994]
    
    [cite_start]// 또는 익명 함수 사용 [cite: 1095]
    p.onmouseover = function () { 
      this.style.backgroundColor = "orchid"; 
    };
    ```
3.  **`addEventListener()` 메소드 사용**:
    * [cite_start]하나의 이벤트에 여러 개의 리스너를 등록할 수 있습니다. [cite: 950]
    ```javascript
    p.addEventListener("mouseover", over); [cite_start]// [cite: 1050]
    ```

## 3. 이벤트 객체 (Event Object)

[cite_start]이벤트가 발생하면, 브라우저는 이벤트에 관한 상세 정보가 담긴 **이벤트 객체**를 생성하여 리스너 함수로 전달합니다. [cite: 1163]

* [cite_start]**전달받기**: 리스너 함수의 첫 번째 매개변수(보통 `e` 또는 `event`)로 전달받습니다. [cite: 1182]
    ```javascript
    [cite_start]function f(e) { // e가 이벤트 객체입니다. [cite: 1184]
      alert(e.type); [cite_start]// 이벤트 종류 (예: "click") [cite: 1206]
    }
    button.onclick = f;
    ```
* **주요 프로퍼티 및 메소드**:
    * [cite_start]`e.type`: 이벤트의 종류 (예: "click", "mouseover"). [cite: 1235]
    * [cite_start]`e.target`: 이벤트를 **실제로 발생시킨** DOM 객체. [cite: 1235, 1238]
    * [cite_start]`e.currentTarget`: 이벤트 리스너가 **현재 실행되고 있는** DOM 객체. [cite: 1235]
    * [cite_start]`e.preventDefault()`: 이벤트의 **디폴트 행동**을 취소시킵니다. [cite: 1235]
        * [cite_start](예: `<a>` 태그의 링크 이동 [cite: 1289][cite_start], `submit` 버튼의 폼 전송 [cite: 1290]을 막음).

## 4. 이벤트 흐름 (Event Flow)

[cite_start]이벤트는 DOM 트리를 따라 흐릅니다. [cite: 1349]

1.  [cite_start]**캡처링 (Capturing) 단계**: `window`에서 시작해 부모 객체를 거쳐 이벤트 타겟까지 **내려갑니다**. [cite: 1351, 1352]
2.  [cite_start]**버블링 (Bubbling) 단계**: 이벤트 타겟에서 시작해 다시 부모 객체를 거쳐 `window`까지 **올라갑니다**. [cite: 1354, 1355]

* [cite_start]`addEventListener(type, listener, true)`: **캡처 리스너**로 등록합니다. [cite: 1417]
* [cite_start]`addEventListener(type, listener, false)`: (기본값) **버블 리스너**로 등록합니다. [cite: 1417, 1418]
* [cite_start]HTML 속성(`onclick=""`)이나 DOM 프로퍼티(`p.onclick=...`) 방식은 **항상 버블 리스너**로 등록됩니다. [cite: 1423]
* [cite_start]`e.stopPropagation()`: 이벤트가 상위 또는 하위 객체로 전파(흐름)되는 것을 중단시킵니다. [cite: 1508, 1509]

## 5. 주요 이벤트 유형

| 이벤트 종류 | 대상 객체 | 설명 |
| :--- | :--- | :--- |
| **마우스 이벤트** | | |
| `onclick` | HTML 태그 | [cite_start]마우스 왼쪽 버튼을 클릭했을 때 [cite: 1515] |
| `onmouseover` | HTML 태그 | [cite_start]마우스가 태그 위로 올라왔을 때 (자식 요소 포함) [cite: 1559] |
| `onmouseout` | HTML 태그 | [cite_start]마우스가 태그 밖으로 나갔을 때 (자식 요소 포함) [cite: 1560] |
| `onmousedown` | HTML 태그 | [cite_start]마우스 버튼을 누르는 순간 [cite: 1559] |
| `onmouseup` | HTML 태그 | [cite_start]눌렀던 버튼을 놓는 순간 [cite: 1559] |
| `onwheel` | HTML 태그 | 마우스 휠을 굴릴 때. [cite_start]`e.wheelDelta` 값으로 방향 확인 (양수: 위, 음수: 아래) [cite: 1562, 1563, 1564] |
| `oncontextmenu` | HTML 태그 | 마우스 오른쪽 버튼 클릭 시. [cite_start]리스너에서 `return false`로 메뉴 차단 가능. [cite: 1699, 1703] |
| **로딩 이벤트** | | |
| `onload` | `window`, `<img>` | [cite_start]`window`: 웹 페이지의 모든 리소스(이미지 포함) 로딩 완료 시. [cite: 1742, 1743] [cite_start]<br> `<img>`: 해당 이미지의 로딩 완료 시. [cite: 1789] |
| **포커스 이벤트** | | |
| `onfocus` | `<input>` 등 | [cite_start]HTML 태그가 포커스를 얻었을 때 (예: 텍스트박스 클릭) [cite: 1925, 1926] |
| `onblur` | `<input>` 등 | [cite_start]HTML 태그가 포커스를 잃었을 때 (예: 다른 곳 클릭) [cite: 1922, 1923] |
| **폼 이벤트** | | |
| `onchange` | `<select>`, `<input>` | [cite_start]`<select>`의 `<option>`이 변경되었을 때. [cite: 2071] |
| `onsubmit` | `<form>` | `<form>` 태그 내의 `submit` 버튼 클릭 시. [cite_start]리스너에서 `return false`로 폼 전송을 막을 수 있음. [cite: 2212, 2213, 2215] |
| `onreset` | `<form>` | `<form>` 태그 내의 `reset` 버튼 클릭 시. [cite_start]리스너에서 `return false`로 폼 초기화를 막을 수 있음. [cite: 2209, 2210, 2215] |
| **키보드 이벤트** | | |
| `onkeydown` | HTML 태그 | [cite_start]키가 눌러지는 순간 (모든 키) [cite: 2116] |
| `onkeypress` | HTML 태그 | [cite_start]문자 키가 눌러지는 순간 (F1, Shift 등 기능키 제외) [cite: 2117, 2118] |
| `onkeyup` | HTML 태그 | [cite_start]눌러진 키가 떼어지는 순간 [cite: 2121] |
